/*function onEdit(e){
  let ss = e.source.getActiveSheet();
  let name = ss.getName();
  if (name == "RawData" || e.range.rowStart == 1) return;
  
  e.range.clearContent();
  console.log(e);
  
  //let db = SpreadsheetApp.getActiveSpreadsheet().getSheetByName("RawData");
  //let r = ss.getRange(e.range.rowStart,1).getValue();
  //db.getRange(r+1,e.range.columnStart).setValue(e.value);
}*/
